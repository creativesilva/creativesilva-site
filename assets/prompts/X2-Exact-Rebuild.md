Run X2 V4.4.

**Image 1 = MASTER.**
**Image 2 = SUPPORT ONLY.**
**DELTA = NONE.**

Before generating anything, perform the full V4.4 preflight audit and lock the master coordinates, exact 10% overlap window, crop boundaries, common scale, horizontal mapping, and true-redraw quality target.

Generate exactly TWO images total and make no retries.

**CALL 1:** `X2_PASS_B_LOWER.png` only. LOWER is the scale anchor. Begin at the calculated `overlap_top`, which must exclude visible chest/bust, and continue through complete shoes plus white margin below feet.

Then perform the between-pass audit without generating another image. Lock the overlap scale witnesses and check for wavy/repeating pseudo-upscale texture.

**CALL 2:** `X2_PASS_A_UPPER.png` only. Use the identical scale and horizontal mapping established by LOWER. End exactly at `overlap_bottom`. Do not zoom in to fill the canvas.

The final shared BODY-CONTENT overlap must be exactly 10% of master character height. Never combine both passes on one canvas. No variants, previews, alternates, or automatic regeneration.

Render both passes as genuine from-scratch high-resolution photographic reconstructions. Preserve the master's identity, intentionally exaggerated proportions, pose, clothing, camera, lighting, and composition exactly. Do not imitate an AI-upscale look. Reject wavy, swirled, tiled, embossed, etched, repeating, moire, sharpened, or fake high-frequency texture.

After CALL 2, stop.

If there is no support image, replace `Image 2 = SUPPORT ONLY` with `No support image`.
